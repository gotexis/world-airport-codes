# Data Scripts

Place your scraper/data pipeline scripts here.

## Convention
- `scrape-*.ts` or `scrape-*.py` — data collection scripts
- Output data to `src/data/` as JSON files
- All data must be real (no mock/synthetic data)
- Data budget: ≤$5 AUD/year
